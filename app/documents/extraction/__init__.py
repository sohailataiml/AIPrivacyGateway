"""Document text extraction.

``models`` defines what extraction produces, ``extractors`` holds the pure
per-type parsing functions, and ``runner`` runs them where a hostile file cannot
take the gateway down with it (ADR-0028).
"""
